#include<stdio.h>

void main()
{
	int a,b;
	printf("\n Enter the number for which the seeds have to be found");
	int number;
	scanf("%d",&number);
  
		for(int i=1;i*i<=number;++i)
		{
  
  
			if(number%i==0)
				{
				a=function(i);
				if(d==number)	  
				printf("\n %d",i);	   
				b=function(number/i);		
				if(number/i!=i && b==number)	  
				printf("\n %d ",(number/i));
				}
		}

}

int function(int no)
{
  int r=n;
  while(no!=0)
	{
		r*=no%10;
		no/=10;
	}
  return r;
}
