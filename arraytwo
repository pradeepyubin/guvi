#include <stdio.h>
 
int main()
{
  int array[100], n, c,e, d, swap,i;
 
  printf("Enter number of elements\n");
  scanf("%d", &n);
 
  printf("Enter %d integers\n", n);
 
  for (c = 0; c < n; c++)
    scanf("%d", &array[c]);
	
	 for ( c = 0 ; c < n ; c++ )
     {
		for ( e = 0 ; e < n ; c++ )
		{	i=0
			if(array[c]==array[e])
			print("\n %d",array[c]);
			break;
		}
	 
	 break;
	 }
	 
	 return 0;
	 
}
